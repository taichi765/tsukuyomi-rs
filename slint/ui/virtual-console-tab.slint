import { Palette } from "std-widgets.slint";
import { MiddleText, LargeText } from "styling.slint";
export component VirtualConsoleTab inherits Rectangle {
    LargeText {
        text: "This is Virtual Console!";
    }
}
