import { TkPalette } from "../styling.slint";
import { TkText } from "../parts/tk-text.slint";
export component FixtureListView inherits Rectangle{
    border-width: 2px;
    border-color: TkPalette.border;
    TkText {
        text: "this is fixture list view";
    }
}