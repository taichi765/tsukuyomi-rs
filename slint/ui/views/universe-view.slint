import { TkPalette } from "../styling.slint";
import { StyleMetrics } from "std-widgets.slint";
import { TkText } from "../parts/tk-text.slint";

export component UniverseView inherits Rectangle {
    border-width: 2px;
    border-color: TkPalette.border;
    
    
    TkText {
        text: "this is universe view";
    }    
}