import { HorizontalBox } from "std-widgets.slint";
export component FixturesTab inherits HorizontalBox {
    Rectangle {
        Text {
            text: "This is fixtures library!";
        }
    }
    Rectangle {
        Text {
            text: "This is DMX Patch!";
        }
    }
}