import { TkStyleMetrics, FontSettings } from "../styling.slint";
import { Palette } from "std-widgets.slint";
export component TkText inherits Text {
    color: Palette.foreground;
    font-size: FontSettings.font-size;
}